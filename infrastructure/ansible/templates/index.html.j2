<!DOCTYPE html>
<html>
<head>
    <title>{{ inventory_hostname }} - Server Info</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .info { border: 1px solid #ddd; padding: 20px; border-radius: 5px; }
        h1 { color: #2c3e50; }
    </style>
</head>
<body>
    <div class="info">
        <h1>Server: {{ inventory_hostname }}</h1>
        <p><strong>Hostname:</strong> {{ ansible_hostname }}</p>
        <p><strong>IP Address:</strong> {{ ansible_default_ipv4.address }}</p>
        <p><strong>Server Type:</strong> 
            {% if inventory_hostname == 'laravel-api' %}
              Laravel API Server
            {% elif inventory_hostname == 'nodejs-app1' or inventory_hostname == 'nodejs-app2' %}
              Node.js Application Server
            {% elif inventory_hostname == 'multi-app1' or inventory_hostname == 'multi-app2' %}
              Multi-purpose Application Server
            {% else %}
              Web Server
            {% endif %}
        </p>
        <p><strong>Date & Time:</strong> {{ ansible_date_time.iso8601 }}</p>
    </div>
</body>
</html>